<div class="container">
    <h1 id="team-identify" value="{{teamid}}">{{teamname}}</h1>

    <h2>Tanks</h2>

    <ul id="tank-list">
        <!--
        {{#each role}}
        {{#if Tank}}
        {{> ?/?-block Healer=false DPS=false}}
        {{/if}}
        {{/each}}
        -->
    </ul>

    <h2>Healers</h2>

    <ul id="heal-list">
        <!--
        {{#each role}}
        {{#if Healer}}
        {{> ?/?-block Tank=false DPS=false}}
        {{/if}}
        {{/each}}
        -->
    </ul>

    <h2>DPS</h2>
    <ul id="dps-list">
        <!--
        {{#each role}}
        {{#if DPS}}
        {{> ?/?-block Healer=false Tank=false}}
        {{/if}}
        {{/each}}
        -->
    </ul>

    <h2>Utility</h2>
    <ul id="util-list">

    </ul>

    <h2>Raid Member</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="char-name">Character Name:</label>
            <input type="text" id="char-name" name="char-name">
        </div>

        <div class="form-group">
            <label for="char-class">Class:</label>
            <select id="char-class" name="char-class">
                <option>Warrior</option>
                <option>Hunter</option>
                <option>Priest</option>
                <option>Rogue</option>
                <option>Mage</option>
                <option>Warlock</option>
                <option>Druid</option>
                <option>Paladin</option>
                <option>Shaman</option>
                <option>Death Knight</option>
                <option>Monk</option>
                <option>Demon Hunter</option>
            </select>
        </div>

        <div class="form-group">
            <label for="char-role">Choose a role:</label>
            <select id="char-role" name="char-role">
                <option>DPS</option>
                <option>Tank</option>
                <option>Healer</option>
                <option>Utility</option>
            </select>
        </div>

        <button type="submit" id="add-member">Add</button>
    </form>

    <button>Save Changes</button>

    <a href="/teamlist">Back to Team List</a>;
</div>

<script type="text/javascript" src="/js/navbar.js"></script>
<script type="text/javascript" src="/js/teaminfo.js"></script>